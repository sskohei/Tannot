# AGENT.md

## このファイルについて

このリポジトリで開発を行うエージェント・開発者向けの作業規約です。プロダクトの目的、技術選定、設計上の判断は [`READE.md`](./READE.md) と [`docs/`](./docs/) を参照してください。

## プロダクト概要

英単語（例: `run`, `give up`）のリストから、英単語・日本語訳・例文を持つ単語帳を作成し、Web Speech APIによる読み上げ付きの間隔反復学習を行う Web アプリです。学習カードでは最初に英単語と英単語音声を提示し、解答後に日本語訳・例文・例文音声を表示します。利用者は「もう一度・難しい・普通・簡単」で想起の結果を評価し、次回復習日を更新します。

## 開発時の基本方針

- まず `READE.md` と関連する `docs/` を読み、記載された責務の境界を守る。
- TypeScript を基本とし、外部入力は API 境界で検証する。
- 認証・認可はサーバー側で必ず行う。クライアントから送られた `userId` を信用しない。
- D1 の SQL はマイグレーションで管理し、既存データを壊す変更には移行方針を添える。
- 秘密情報、ユーザーの OAuth トークン、決済情報をログに出力しない。
- 辞書・例文・音声の出典とライセンス表示を機能として扱い、データを追加したら帰属情報も更新する。
- 日時は保存時に UTC、表示時に利用者のローカルタイムゾーンを使う。
- 既存の変更を上書き・削除しない。大規模な変更は小さなコミット単位に分ける。

## 想定ディレクトリ

```text
app/                 Next.js の画面・ルート
components/          UI コンポーネント
lib/                 共有の型、検証、クライアント処理
server/               Hono の API、認証・決済・サービス層
db/migrations/        Cloudflare D1 のマイグレーション
public/               静的ファイル
scripts/              辞書・例文・音声データの取り込み補助
docs/                 設計・運用・ライセンス文書
```

## 実装・検証のルール

- 変更に対応するテストを追加または更新する。特に復習間隔、認可、Stripe webhook の冪等性を優先する。
- `npm run lint`、`npm run typecheck`、`npm test`（存在する場合）を実行する。
- Cloudflare 固有コードは、可能な範囲で Wrangler のプレビュー環境でも確認する。
- API の変更時は [`docs/api.md`](./docs/api.md) を更新する。
- スキーマの変更時は [`docs/database.md`](./docs/database.md) とマイグレーションを同時に更新する。
- 仕様が未確定の場合は、安全なデフォルトを採用し、判断を文書に残す。

## セキュリティ上の注意

- Google OAuth の client secret、Better Auth の secret、Stripe secret key、webhook secret は `.env` や Cloudflare の secret に置く。
- ブラウザへ公開してよい値とサーバー専用の値を分離する。
- 単語帳・カード・学習記録は所有者を条件にしたクエリで取得・更新・削除する。
- 単語帳作成 API には入力件数、文字数、レート制限を設ける。
- Stripe のイベントは署名検証後に処理し、イベント ID を保存して二重処理を防ぐ。
